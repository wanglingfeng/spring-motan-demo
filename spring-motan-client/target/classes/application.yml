server:
  port: 8080

spring:
  output.ansi.enabled: always

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false
    
management:
  security:
    enabled: false